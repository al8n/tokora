# Probes where both sides run the CONSUMER's item, i.e. the added name was never a
# candidate. That is sometimes the honest answer — a bound genuinely rejects the receiver —
# but it is indistinguishable from a vacuous probe, so each one is justified here in
# writing or the gate fails.
#
# Ten of the thirteen glob probes once sat in this state while reporting `ok`: four macro
# names whose template globbed nothing, three names in `tokora::parser`/`tokora::cst` whose
# probe globbed only the crate root, and three functions referenced in the type namespace.
# None of them was tested. That is why agreement is no longer accepted by default.
#
# ── `InputRef::recursion` — an EARLIER pick claims the call ──────────────────────────────
#
# tokora's is `pub const fn recursion(&self) -> &RecursionLimiter`. The `inherent_method`
# template's consumer is `fn recursion(&mut self) -> u8`, and the probe's receiver expression
# already has type `&mut InputRef<..>`. The receiver walk therefore finds the consumer's item
# at its FIRST step — `self: &mut InputRef` matches by value — while tokora's `&self` item is
# only reachable one autoref step later. The earlier pick wins, on both sides, and tokora's
# item is never a candidate.
#
# So this is a real property of this probe's shape and not a broken probe — and it is a
# statement about the SHAPE, not about the name. A consumer whose extension item takes `&self`
# competes at the same step tokora's does, and this harness cannot generate that consumer:
# `inherent_method` hardcodes `&mut self`, exactly as it hardcodes `-> u8` (see README, "What
# this harness does NOT test"). Read these two rows as "the `&mut self` consumer keeps its
# call", never as "`recursion` cannot be stolen".
#
# The sibling names on the same owner are the control: `descend` and `descending` are both
# `&mut self` in tokora, compete at the same first step, and score `loud` on this same
# template — so the template does construct collisions on `InputRef` when the receivers agree.
recursion/used
recursion/discarded

# ── `InputRef::token_budget` — the identical shape as `recursion`, same owner ────────────
#
# tokora's is `pub const fn token_budget(&self) -> &TokenBudgetTally` — the doc on it says so
# directly: "The same absence [`recursion`] relies on." Same owner, same receiver kind, same
# `inherent_method` template, so the analysis above applies verbatim: the probe's receiver
# expression already has type `&mut InputRef<..>`, the consumer's `fn token_budget(&mut self)
# -> u8` matches it at the FIRST step of the walk, and tokora's `&self` item is one autoref
# step later and never a candidate. The earlier pick wins, on both sides.
#
# Read these two rows exactly as the `recursion` rows: "the `&mut self` consumer keeps its
# call", never as "`token_budget` cannot be stolen". `recursion` is itself the control this
# time — same owner, same template, same outcome, for the same stated reason.
token_budget/used
token_budget/discarded

# ── `InputRef::{trip_snapshot, tripped_during_attempt}` — the `recursion` shape, third time ─
#
# Both are `pub fn ..(&self, ..)` on `InputRef`, so the analysis on `recursion` above transfers
# without amendment: the `inherent_method` template's consumer is `fn NAME(&mut self) -> u8`, the
# probe's receiver expression already has type `&mut InputRef<..>`, and the consumer's item is
# therefore found at the FIRST step of the receiver walk while tokora's `&self` items are one
# autoref step later. The earlier pick wins on both sides and tokora's item is never a candidate.
# `recursion` and `token_budget` are the controls — same owner, same receiver kind, same template,
# same outcome — and `descend`/`descending` on the same owner are the counter-control: both are
# `&mut self`, compete at the same first step, and score `loud`.
#
# Read these four rows exactly as those: **"the `&mut self` consumer keeps its call"**, never as
# "these names cannot be stolen". They can. A consumer whose extension item takes `&self` competes
# at the same step tokora's does and would lose it, and this harness cannot generate that consumer
# — `inherent_method` hardcodes `&mut self` (see README, "What this harness does NOT test"). That
# case is therefore not covered by a probe at all; it is **disclosed** instead, in the CHANGELOG's
# "Source-breaking additions that can change behaviour with no diagnostic at the call site"
# section, with the UFCS spelling that restores the consumer's item.
#
# `tripped_during_attempt` additionally takes an argument where the template's consumer takes
# none, so even a `&self` consumer would be an arity mismatch rather than a silent steal for the
# ONE-argument spelling — but the paired `trip_snapshot`/`tripped_during_attempt` call the
# CHANGELOG describes passes the baseline through inference, and that is the shape that can switch
# silently. The disclosure is written against that shape, not against this template's.
trip_snapshot/used
trip_snapshot/discarded
tripped_during_attempt/used
tripped_during_attempt/discarded

# ── `InputRef::at_scanner_stop` — the `recursion` shape, fourth time ─────────────────────
#
# `pub fn at_scanner_stop(&self) -> bool` on `InputRef`, so the analysis on `recursion` above
# transfers unamended: the `inherent_method` template's consumer is `fn NAME(&mut self) -> u8`, the
# probe's receiver expression already has type `&mut InputRef<..>`, and the consumer's item is
# found at the FIRST step of the receiver walk while tokora's `&self` item is one autoref step
# later. The earlier pick wins on both sides and tokora's item is never a candidate. Same controls:
# `recursion` / `token_budget` / the `trip_snapshot` pair for the shape, `descend` / `descending`
# on the same owner as the counter-control that scores `loud` when the receivers agree.
#
# Read these two rows exactly as those: **"the `&mut self` consumer keeps its call"**, never as
# "this name cannot be stolen". It can, by a consumer whose extension item takes `&self`, and the
# harness cannot generate that consumer. That case is disclosed in the CHANGELOG's "Source-breaking
# additions that can change behaviour with no diagnostic at the call site" section, with the UFCS
# spelling that restores the consumer's item.
#
# This one takes NO argument where `tripped_during_attempt` takes one, so there is no inferred
# argument type to hide which item won — the disclosure is written against a `bool` in a boolean
# position, which is the shape that can still switch silently.
at_scanner_stop/used
at_scanner_stop/discarded

# ── `InputRef::scanner_trip_snapshot` / `scanner_stopped_during_attempt` — the same shape ─
#
# `pub fn scanner_trip_snapshot(&self) -> ScannerTripBaseline<'_>` and
# `pub fn scanner_stopped_during_attempt(&self, since: ScannerTripBaseline<'_>) -> bool`, both
# `&self` on `InputRef`, so the `recursion` analysis transfers unamended for the fifth and sixth
# time: the `inherent_method` template's consumer is `fn NAME(&mut self) -> u8`, the probe's
# receiver expression already has type `&mut InputRef<..>`, the consumer's item is found at the
# FIRST step of the receiver walk and tokora's `&self` item one autoref step later. The earlier
# pick wins on both sides. Same controls as the descent twins directly above — `recursion` /
# `token_budget` for the shape, `descend` / `descending` on the same owner as the counter-control
# that scores `loud` when the receivers agree.
#
# These are the **scanner** twins of `trip_snapshot` / `tripped_during_attempt`, and the paragraph
# on those rows applies word for word, including the part that matters: the paired call passes the
# baseline through inference, so a consumer whose extension item takes `&self` could take the call
# with no diagnostic. That is disclosed in the CHANGELOG's "Source-breaking additions that can
# change behaviour with no diagnostic at the call site" section, with the UFCS spelling that
# restores the consumer's item — the two new names are added to the same entry rather than given
# one of their own, because the analysis and the remedy are identical.
scanner_trip_snapshot/used
scanner_trip_snapshot/discarded
scanner_stopped_during_attempt/used
scanner_stopped_during_attempt/discarded

# ── `InputRef::scanner_attempt` / `scanner_outcome` — the same shape, seventh and eighth ──
#
# `pub fn scanner_attempt(&self) -> ScannerAttempt<'_, '_, L>` and
# `pub fn scanner_outcome(&self, since: &ScannerAttempt<'_, '_, L>) -> ScannerOutcome`, both `&self`
# on `InputRef`. The `recursion` analysis transfers unamended once more: the `inherent_method`
# template's consumer is `fn NAME(&mut self) -> u8`, the probe's receiver expression already has
# type `&mut InputRef<..>`, the consumer's item is found at the FIRST step of the receiver walk and
# tokora's `&self` item one autoref step later, so the earlier pick wins on both sides and tokora's
# item is never a candidate. Same controls as the four rows above.
#
# `scanner_outcome` returns an ENUM rather than a `bool` or a `u8`, so a consumer's `-> u8`
# extension item would be a type mismatch at most call sites rather than a silent steal — but the
# paired call passes the capture through inference exactly as the two pairs above do, which is the
# shape that can still switch with no diagnostic. Disclosed with them in the CHANGELOG's
# "Source-breaking additions that can change behaviour with no diagnostic at the call site" section,
# with the UFCS spellings that restore the consumer's items.
scanner_attempt/used
scanner_attempt/discarded
scanner_outcome/used
scanner_outcome/discarded

# ── `Emitter::commit_lexer_error` — the SAME rule, in the mirror ──────────────────────────
#
# tokora's is `fn commit_lexer_error(&mut self, ..)` on the `Emitter` trait. The
# `trait_method` template's consumer is `impl<T> ConsumerComb for T`, so it supplies a
# candidate at EVERY pick, and its three spellings declare `self` (`same_pick`) or `&self`
# (`later_pick_used`, `later_pick_discarded`). The subject is a `Silent<PErr>` VALUE, which
# derefs to nothing, so there is exactly one step and three picks on it: by value, `&`, then
# `&mut`. The consumer's item sits at the first or the second; tokora's sits at the third.
# The earlier pick wins, on both sides, and tokora's item is never a candidate.
#
# That is the `InputRef::recursion` rows above with the receiver kinds swapped — there the
# consumer's `&mut self` beat tokora's `&self`, here the consumer's `self`/`&self` beat
# tokora's `&mut self`. Same rule: an earlier pick claims the call.
#
# Read it as a statement about the SHAPE, never as "`commit_lexer_error` cannot be stolen".
# The three spellings are named for a tokora method taken BY VALUE — `ParseInput::labelled`,
# where `same_pick` really is the same pick and `&self` really is a later one. Against a
# `&mut self` trait method on a non-deref receiver there IS no later pick, so the spelling set
# cannot express a competing consumer at all: the one that WOULD compete declares `&mut self`
# and collides at the same pick (E0034, loud), and `trait_method` hardcodes the three above.
# Generating it is the same unbounded enumeration the README declines twice; recording the gap
# is the instrument this repository trusts.
#
# The subject is not the vacuous half of that pair. `Silent<PErr>` is the fixture's own
# emitter — `PCtx` is built on it — so tokora really does implement `Emitter` for it, and
# `emitter_value()` in `gen_probe.py` is what pins that down rather than leaving `recvr` to a
# default. A subject that did NOT implement the trait would produce this same agreement while
# meaning nothing, which is the failure `TRAITS` exists to refuse.
commit_lexer_error/same_pick
commit_lexer_error/later_pick_used
commit_lexer_error/later_pick_discarded

# ── `Diagnose` / `DiagnoseExt` — tokora implements NEITHER, for any type ─────────────────
#
# Every row below agrees on both sides, and the reason is one API fact rather than a property
# of this probe's shape: `tokora::diagnostic` publishes a contract for a CONSUMER's error types
# to implement, and tokora implements it for none of its own. `DiagnoseExt` is blanket over
# `D: Diagnose`, so it reaches exactly as far — which is nowhere. tokora's items are therefore
# candidates on NO receiver, on either side, and no subject this harness can name would change
# that.
#
# `gen_probe.py` rides `UnexpectedEot::eot(7usize)` deliberately: it is one of tokora's own
# error types, the natural first candidate for a `Diagnose` impl, so the agreement says "even
# there, tokora's item is not a candidate" rather than "some unrelated value does not implement
# it". Read the README's bound on `TRAITS` records before reading the verdict column — a subject
# that does not implement the trait produces a clean run that means nothing, and here that is
# the finding, not a mis-chosen subject.
#
# What the rows are evidence FOR, stated narrowly: no call site that already exists can change
# which item it resolves to. Reaching `Diagnose::code` at all costs a consumer two source
# changes they make themselves — implementing the trait for their own type, and importing the
# trait, which is not in any prelude. Neither happens because tokora published a name.
#
# RE-OPEN CONDITION: the first time tokora implements `Diagnose` for one of its own types,
# `TRAITS["Diagnose"]["recvr"]` becomes a value of THAT type and these rows are re-probed. Until
# then they are trusted, not verified, and this paragraph plus the reviewer who agreed with it
# are the whole of their protection.
code/same_pick
code/later_pick_used
code/later_pick_discarded
severity/same_pick
severity/later_pick_used
severity/later_pick_discarded
primary/same_pick
primary/later_pick_used
primary/later_pick_discarded
primary_label/same_pick
primary_label/later_pick_used
primary_label/later_pick_discarded
labels/same_pick
labels/later_pick_used
labels/later_pick_discarded
label/same_pick
label/later_pick_used
label/later_pick_discarded
path_segments/same_pick
path_segments/later_pick_used
path_segments/later_pick_discarded
path_segment/same_pick
path_segment/later_pick_used
path_segment/later_pick_discarded
help/same_pick
help/later_pick_used
help/later_pick_discarded
labels_iter/same_pick
labels_iter/later_pick_used
labels_iter/later_pick_discarded
path_segments_iter/same_pick
path_segments_iter/later_pick_used
path_segments_iter/later_pick_discarded

# ── `ErrorContainer::clear` — the `&mut self` trait method, for the third time ────────────
#
# tokora's is `fn clear(&mut self)`, defaulted on the `ErrorContainer` trait. The subject is
# `Option::<PErr>::None`, one of the four types tokora implements the trait for, chosen over
# `Vec`/`VecDeque` because those carry an inherent `clear` of their own that would confound the
# walk, and over a `ArrayDeque` because it needs construction. `Option` derefs to
# nothing, so there is exactly one step and three picks on it: by value, `&`, then `&mut`. The
# `trait_method` template's consumer is `impl<T> ConsumerComb for T`, supplying a candidate at
# every pick, and its three spellings declare `self` or `&self` — the first and second picks.
# tokora's sits at the third. The earlier pick wins, on both sides, and tokora's item is never
# a candidate.
#
# This is `Emitter::commit_lexer_error` exactly: same rule, same trait shape, same conclusion.
# The verdict is FOREKNOWN from the receiver kinds, which is #225's ruling — the consumer that
# would genuinely compete declares `&mut self`, collides at the same pick, and is E0034-loud,
# and `trait_method` cannot generate it.
#
# Read these three as "the `self`/`&self` consumer keeps its call", never as "`clear` cannot be
# stolen". The `&mut self` competitor is real and is disclosed on the INHERENT side of the same
# change: `Errors::clear` is measured SILENT in `disclosed.txt`, on the same name, because
# there tokora's item and the consumer's do meet at one pick.
clear/same_pick
clear/later_pick_used
clear/later_pick_discarded
# ── #282's read frontier — the by-value pick, and one `&mut self` item ────────────────────
#
# Four of this release's trait rows agree on both sides, and they agree for the two reasons
# already written up above rather than for a new one. The rest score `loud`, on the same
# templates and the same subjects, which is what says these four are a property of their SHAPE
# and not a broken probe or a mis-chosen receiver.
#
# `*/same_pick` — `read_frontier`, `take_probe`. The `trait_method` template's `same_pick`
# consumer declares `self` BY VALUE, which is the first step of the receiver walk. tokora's two
# items declare `&self` and `&mut self`, both of them later steps. The subjects are
# `PLexer<'static>` and `()`, neither of which derefs to anything, so there is no earlier step
# for tokora's item to be found at. The consumer's item claims the call on both sides and
# tokora's is never a candidate. The control is in the same run: the `later_pick_*` rows for
# `read_frontier` put the consumer at `&self`, where tokora's item also sits, and they go
# `loud` — so this template does construct collisions on these subjects when the receivers
# agree.
#
# `take_probe` is the value channel, and it replaces the `probe`/`clear_probe` PAIR this branch
# first shipped — one consuming operation, because two independently defaulted members could be
# half-implemented (override the reader, inherit the empty reset) and the adapter then accepted
# a value no scan on that lexer recorded. For this harness that is a rename plus a deletion:
# `take_probe` has `clear_probe`'s receiver (`&mut self`), arity (zero) and subject (`()`), so
# the analysis below is `clear_probe`'s unchanged and the rows are carried over rather than
# re-derived. The `probe/same_pick` row goes with the name; `probed_to` remains an inherent
# method on `Probe`, a different owner and a different template, and needs no entry here
# because `Probe` is minted by this same diff and its rows can only reach `new-owner`.
#
# `take_probe/later_pick_used`, `take_probe/later_pick_discarded` — this is issue #225's
# foreknown-vacuous shape verbatim: a `&mut self` trait method on a non-deref subject. The
# consumer's `&self` is pick two and tokora's `&mut self` is pick three, so the consumer wins
# the call whatever the receiver is, and #225's ruling declines to build the `&mut self`
# consumer that would compete at pick three. Read these two rows as "the `&self` consumer keeps
# its call", never as "`take_probe` cannot be stolen".
#
# Why the subjects satisfy the bounds, since `recvr` is trusted and not verified: `PLexer` is
# the fixture's `LogosLexer<'static, Tok>`, which tokora implements `Lexer` for through the
# adapter's blanket impl over `T: FromLogos` — `Tok` derives `Logos` and implements `Token`,
# which is what that impl asks for. `()` is tokora's OWN `State` implementor (`impl State for
# ()` in `state/mod.rs`), and it is what a `logos` vocabulary declaring no `extras` actually
# uses, which `Tok` is.
read_frontier/same_pick
take_probe/same_pick
take_probe/later_pick_used
take_probe/later_pick_discarded

# ── `TrackerExt` / `RecursionTrackerExt` — the SAME rule, TWO independent reasons ──────────
#
# tokora's items are `fn increase_token_and_decrease_recursion(&mut self)`,
# `fn increase_token_and_decrease_recursion_and_check(&mut self) -> Result<..>`,
# `fn increase_token_and_check(&mut self) -> Result<..>`, `fn increase_both(&mut self)` and
# `fn increase_both_and_check(&mut self) -> Result<..>` on `TrackerExt`, and
# `fn increase_and_check(&mut self) -> Result<..>` on `RecursionTrackerExt` — every one of
# them `&mut self`, with no exception. The `trait_method` template's consumer is
# `impl<T> ConsumerComb for T`, so it supplies a candidate at EVERY pick, and its three
# spellings declare `self` (`same_pick`) or `&self` (`later_pick_used`,
# `later_pick_discarded`). `Limiter` and `RecursionLimiter` are plain structs that deref to
# nothing, so there is exactly one step and three picks on each: by value, `&`, then `&mut`.
# The consumer's item sits at the first or the second; tokora's sits at the third. The
# earlier pick wins, on both sides, and tokora's item is never a candidate — this is
# `Emitter::commit_lexer_error` and `ErrorContainer::clear` exactly: same rule, same trait
# shape, same conclusion, and the same #225 foreknown-vacuous class. The consumer that would
# genuinely compete declares `&mut self`, collides at the same pick, is E0034-loud, and
# `trait_method` cannot generate it — see #225's ruling before extending the generator to try.
#
# Read this as a statement about the SHAPE, never as "these six names cannot be stolen".
#
# ── A second, independent reason, which `commit_lexer_error` and `clear` do not have ──────
#
# `TrackerExt` and `RecursionTrackerExt` are traits THIS diff adds (tokora#265), so — per the
# file header's rule for an added trait — each is reached through a glob over its own module
# rather than a named import: `Tracker` and `RecursionTracker` are pre-existing, so `use
# tokora::state::tracker::*;` / `use tokora::state::recursion_tracker::*;` resolve on the base
# ref too, where a direct `use ...::TrackerExt;` would not. But those MODULES are not new —
# only the trait inside each is — and on the base ref `Tracker` and `RecursionTracker` still
# declare these same six names as their OWN `&mut self` PROVIDED methods (read directly off
# the base ref's source, not assumed: tokora#265's whole point is moving them off
# `Tracker`/`RecursionTracker` — where a type could override one and disambiguate to the
# wrong trait's `check`, the bug this diff fixes — onto these two blanket-implemented
# extension traits instead, where that is unrepresentable). So the same glob that reaches
# `TrackerExt` on head also reaches `Tracker`'s old defaults on base; `Limiter` and
# `RecursionLimiter` implement `Tracker` and `RecursionTracker` on BOTH sides; and base
# compiles and runs (`witness=1`) for a reason that has nothing to do with the receiver-kind
# analysis above.
#
# The two reasons are independent and both hold. A reader who repaired only the receiver-kind
# mismatch — by, say, imagining a future `trait_method` able to generate a `&mut self`
# consumer — would still find these six rows agreeing, because the old provided methods on
# `Tracker`/`RecursionTracker` are the SAME NAMES answering on base regardless of what the
# consumer declares. Do not read a repair of one reason as grounds to expect this row to
# start probing.
#
# ── Control: none exists for this entry, and that is stated rather than left unstated ─────
#
# Every entry above this one that can name a control does: `recursion`/`token_budget` point at
# `descend`/`descending` scoring `loud` on the same owner with matching receivers;
# `read_frontier`/`take_probe` point at their OWN `later_pick_*` rows, since `read_frontier`
# itself is `&self` and so has a same-run sibling that goes `loud`; `ErrorContainer::clear`
# points at `Errors::clear`, the same NAME on a different owner, measured SILENT in
# `disclosed.txt` because there the receiver kinds do align. None of those three shapes is
# available here — checked, not assumed:
#
#   * no same-run control: EVERY spelling of EVERY one of these six names is `&mut self` in
#     tokora, so there is no sibling spelling at a matching pick the way `read_frontier`'s
#     `later_pick_*` rows are for `read_frontier/same_pick`;
#   * no same-owner sibling control: the other five names on the same trait are in the
#     identical foreknown-vacuous boat, so none of them can vouch for another;
#   * no same-name sibling control: grepped the whole crate — none of these six names is
#     declared anywhere else, inherent or trait, so there is no `Errors::clear`-shaped SILENT
#     disclosure to point at either.
#
# The honest best available is `Emitter::commit_lexer_error`'s own rows, cited above as the
# mirror — and that is what this paragraph is doing, the same move its own entry makes
# ("the `InputRef::recursion` rows above with the receiver kinds swapped"). Checked, not
# assumed, that this is not a lesser standard than `commit_lexer_error` was held to:
# `commit_lexer_error` names no same-owner, same-subject `loud` row either. `Emitter` has
# exactly one `&self` item, `bound_source`, and `Emitter::bound_source` — the TRAIT method,
# as opposed to `EmitterView`'s inherent forwarder of the same name, a different owner and a
# different template — has never been probed by this harness at all (checked: absent from
# both `no_collision.txt` and `disclosed.txt` under its bare name), so it cannot serve as a
# control there either. What `commit_lexer_error` rests on is the GENERAL mechanism — an
# earlier pick wins — demonstrated on a DIFFERENT owner (`InputRef::recursion`/`descend`) with
# the receiver kinds swapped; that is the same evidentiary weight this entry rests on, not a
# step down from it.
#
# `recvr` is trusted, not verified (see the file header on `TRAITS`): `Limiter` genuinely
# implements `Tracker` (`impl Tracker for Limiter` in `state/tracker/mod.rs`) and
# `RecursionLimiter` genuinely implements `RecursionTracker` (`impl RecursionTracker for
# RecursionLimiter` in `state/recursion_tracker/mod.rs`) — each is tokora's own, direct
# implementor from the trait's own module, not a convenient stand-in, and both constructors
# (`Limiter::new()`, `RecursionLimiter::new()`) are public and argument-free.
increase_token_and_decrease_recursion/same_pick
increase_token_and_decrease_recursion/later_pick_used
increase_token_and_decrease_recursion/later_pick_discarded
increase_token_and_decrease_recursion_and_check/same_pick
increase_token_and_decrease_recursion_and_check/later_pick_used
increase_token_and_decrease_recursion_and_check/later_pick_discarded
increase_token_and_check/same_pick
increase_token_and_check/later_pick_used
increase_token_and_check/later_pick_discarded
increase_both/same_pick
increase_both/later_pick_used
increase_both/later_pick_discarded
increase_both_and_check/same_pick
increase_both_and_check/later_pick_used
increase_both_and_check/later_pick_discarded
increase_and_check/same_pick
increase_and_check/later_pick_used
increase_and_check/later_pick_discarded

# ── `RecoveryState` — the BY-VALUE consumer keeps its call, on a new owner ────────────────
#
# tokora's are `fn status(&self) -> Status` and three defaulted `fn is_*(&self) -> bool` on
# `RecoveryState`. The `trait_method` template's `same_pick` consumer declares `self` BY VALUE,
# which is the FIRST step of the receiver walk; the subject is `Keyword<&'static str>`, which
# derefs to nothing, so there is no earlier step for tokora's `&self` item to be found at — it
# sits at the step-0 autoref, one pick later. The consumer's item claims the call and tokora's
# is never a candidate. rustc says exactly that in the same log, and the fifth witness reads it:
# `unused import: `new_module::RecoveryState``.
#
# This is `read_frontier/same_pick`'s analysis verbatim, on an owner the same diff introduces.
# The verdict differs — `new-owner-nc*` rather than `ok*` — only because a new owner has no
# before-state to agree WITH; the fact being justified is identical and so is this file's job
# for it.
#
# The control is in the same run and it is what says this is the SHAPE and not a broken probe:
# the `later_pick_*` rows put the consumer at `&self`, the same pick tokora's item sits at, and
# every one of them scores `new-owner-err` — rustc reporting E0034 and naming both candidates.
# So the template does construct a collision on this subject the moment the receiver kinds
# agree. Read these four rows as "the by-value consumer keeps its call", never as "`status`,
# `is_valid`, `is_error` and `is_missing` cannot be stolen".
#
# Why the subject satisfies the bound, since `recvr` is trusted and not verified: `Keyword` is
# one of the two types tokora implements `RecoveryState` for in product code
# (`types/keyword.rs`), and it is chosen over `Ident` — the other one — because `Ident` carries
# PRE-EXISTING inherent `is_valid`/`is_error`/`is_missing`, which take the call and produce a
# `witness=0` that measures nothing. That was measured, not reasoned; see the subject note in
# gen_probe.py.
status/same_pick
is_valid/same_pick
is_error/same_pick
is_missing/same_pick
